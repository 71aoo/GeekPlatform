<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.syclover.geekPlatform.dao.CategoryMapper">

    <!-- 根据id查询某个分类 -->
    <select id="getById" resultMap="allColumnsMapper">
        select * from Categories where id = #{id}
    </select>

    <!-- 获取所有分类 -->
    <select id="getAll" resultMap="allColumnsMapper">
        select * from Categories
    </select>

    <!-- 插入一个category -->
    <insert id="addCategory">
        insert into Categories("name") values(#{name})
    </insert>

    <!-- 所有字段映射 -->
    <resultMap id="allColumnsMapper" type="com.syclover.geekPlatform.entity.Category">
        <result column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="count" property="count"/>
    </resultMap>


</mapper>